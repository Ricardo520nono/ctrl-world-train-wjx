# Deprecated Ctrl-World daily scripts

本目录只保留历史入口，用于复现旧实验或查阅旧路径。

- `20260709/`：legacy Ctrl-World / StarVLA rollout 入口，使用旧的路径和旧的 action 配置。
- `20260718/`：一次性双任务 precompute 入口，已被按任务增量 cache 方案替代。

正式训练、precompute 和评测请使用对应日期目录下的当前脚本，不要从这里启动新 run。
